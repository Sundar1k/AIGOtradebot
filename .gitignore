# secrets — copy .env.example to .env and fill in (never commit .env)
.env
.env.*
!.env.example

# logs
log/
*.log

# python
__pycache__/
*.pyc
.venv/
venv/

# market data — 3-min bar CSVs are ~40MB/symbol; regenerate with backfill_bars.py
data/

# model training checkpoints (not distributed)
finetune/
sml_exp/

# dev / local artifacts
.specify/
graphify-out/
.pytest_cache/
proba_cache.npz

# macOS
.DS_Store
